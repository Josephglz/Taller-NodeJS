<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    {{> header}}

    {{#if status}}
        <div class="flex flex-wrap ml-18 mr-18 mb-5 justify-center select-none">
            {{#each data}}
                <a href="/detalles/{{id}}">
                    <div class="flex flex-col m-2 p-3 w-[290px] bg-slate-200 rounded-md shadow-md hover:animate-pulse">
                        <p class="w-full truncate">ID: {{id}}</p>
                        <p class="w-full truncate">Asignatura: {{asignatura}}</p>
                        <p class="w-full truncate">Docente: {{docente}}</p>
                        <p class="w-full truncate">Duración: {{duracion}}</p>
                        <p class="w-full truncate">Horario: {{horario}}</p>
                    </div>
                </a>
            {{/each}}
        </div>
    {{else}}
        <h2 class="text-2xl text-slate-800 font-bold m-5">No hay datos disponibles</h2>
    {{/if}}
</body>
</html>