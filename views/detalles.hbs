<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    {{> header}}

    <div class="ml-32 mr-32 mt-10 mb-10 bg-slate-200 p-5 rounded-md shadow-sm">
        <div class="flex flex-row items-center">
            <h2 class="w-[80%] text-4xl font-bold text-slate-700">{{data.asignatura}}</h2>
            <h2 class="w-[20%] text-xl text-slate-500 font-medium text-right">{{fecha data.fecha}}</h2>
        </div>
        
        <h2 class="text-sm text-slate-700">Docente: {{data.docente}}</h2>
        <h2 class="text-sm text-slate-700">Horario: {{data.horario}}</h2>
        <h2 class="text-sm text-slate-700">Duración: {{data.duracion}}</h2>

        <div class="flex flex-wrap mt-12 justify-start">
            {{#if imgCont}}
                {{#each imagenes}}
                    <div class="w-[357px] h-80 m-2 bg-white rounded-lg shadow-md">
                        <img class="rounded-t-lg mx-auto" src="/public/imagenes/{{nombre}}" alt="">
                        <div class="p-5">
                            <span class="bg-green-100 text-green-800 text-xs font-semibold mr-2 px-2.5 py-0.5 rounded dark:bg-green-200 dark:text-green-900">Apuntes</span>
                            <p class="mb-3 mt-3 font-normal text-gray-700">{{descripcion}}</p>
                        </div>
                    </div>
                {{/each}}
            {{else}}
                <h2 class="text-2xl text-slate-700 font-medium">No hay imagenes disponibles</h2>
            {{/if}}
        </div>
    </div>
</body>
</html>